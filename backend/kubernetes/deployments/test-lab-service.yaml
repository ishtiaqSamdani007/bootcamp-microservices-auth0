# deployment for spring boot microservice test-lab-service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-lab-service
  labels:
    role: test-lab-service
    env: demo
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 25%
  selector:
    matchLabels:
      role: test-lab-service
  template:
    metadata:
      labels:
        role: test-lab-service
    spec:
      containers:
        - name: test-lab-service-container
          image: ghcr.io/ishtiaqsamdani007/auth0-bootcamp-backend-test-lab-service:v26
          imagePullPolicy: Always
          ports:
            - containerPort: 9006
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret
          env:
            - name: EUREKA_INSTANCE_HOSTNAME
              value: test-lab-service
      
      imagePullSecrets:
        - name: my-docker-secret