# deployment for spring boot microservice test-service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-service
  labels:
    role: test-service
    env: demo
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 25%
  selector:
    matchLabels:
      role: test-service
  template:
    metadata:
      labels:
        role: test-service
    spec:
      containers:
        - name: test-service-container
          image: ghcr.io/ishtiaqsamdani007/auth0-bootcamp-backend-test-service:v26
          imagePullPolicy: Always
          ports:
            - containerPort: 9007
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret
          env:
            - name: EUREKA_INSTANCE_HOSTNAME
              value: test-service
      
      imagePullSecrets:
        - name: my-docker-secret