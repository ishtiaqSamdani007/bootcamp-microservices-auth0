# deployment for spring boot microservice card-service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: card-service
  labels:
    role: card-service
    env: demo
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 25%
  selector:
    matchLabels:
      role: card-service
  template:
    metadata:
      labels:
        role: card-service
    spec:
      containers:
        - name: card-service-container
          image: ghcr.io/ishtiaqsamdani007/auth0-bootcamp-backend-card-service:v26
          imagePullPolicy: Always
          ports:
            - containerPort: 9003
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret
          env:
            - name: EUREKA_INSTANCE_HOSTNAME
              value: card-service
      
      
      imagePullSecrets:
        - name: my-docker-secret