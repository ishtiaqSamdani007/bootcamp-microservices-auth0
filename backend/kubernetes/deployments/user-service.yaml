# deployment for spring boot microservice user-service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  labels:
    role: user-service
    env: demo
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 25%
  selector:
    matchLabels:
      role: user-service
  template:
    metadata:
      labels:
        role: user-service
    spec:
      containers:
        - name: user-service-container
          image: ghcr.io/ishtiaqsamdani007/auth0-bootcamp-backend-user-service:v26
          imagePullPolicy: Always
          ports:
            - containerPort: 9008
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret
          env:
            - name: EUREKA_INSTANCE_HOSTNAME
              value: user-service
      
      imagePullSecrets:
        - name: my-docker-secret