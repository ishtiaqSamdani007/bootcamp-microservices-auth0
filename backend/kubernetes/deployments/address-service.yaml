# deployment for spring boot microservice address-service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: address-service
  labels:
    role: address-service
    env: demo
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 25%
  selector:
    matchLabels:
      role: address-service
  template:
    metadata:
      labels:
        role: address-service
    spec:
      containers:
        - name: address-service-container
          image: ghcr.io/ishtiaqsamdani007/auth0-bootcamp-backend-address-service:v26
          imagePullPolicy: Always
          ports:
            - containerPort: 9001        
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret
          env:
            - name: EUREKA_INSTANCE_HOSTNAME
              value: address-service
      
      imagePullSecrets:
        - name: my-docker-secret